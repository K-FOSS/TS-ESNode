name: Code Quality

on: [push, pull_request]

jobs:
  Prettier:
    name: Prettier
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: k-foss/npm-run-action@master
        with:
          scriptName: 'prettier'
